{{>sidenav}}
<div class="col-tog single-col">

    <div class="app">
        {{>topbar}}
        {{>past-events-list-header}}
        {{>past-events-filter-content}}
        {{>past-events-list}}
    </div>

</div>

<div class="col-tog double-col" hidden>

    <div class="side-col">
        <div class="side-col-scroll">
        {{>scroll-title-home}}

        </div>
    </div>

    <div class="app double">
        {{>topbar-hidden}}
        {{>past-events-list-header}}
        {{>past-events-filter-content}}
    </div>

</div>
