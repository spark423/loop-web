{{>sidenav}}
<div class="col-tog single-col">

    <div class="app">
        {{>topbar}}
        {{>new-org-invite-header}}
        {{>new-org-add-members}}

    </div>

</div>

<div class="col-tog double-col" hidden>

    <div class="side-col">
        <div class="side-col-scroll">
        {{>scroll-title-home}}
        </div>
    </div>

    <div class="app double">
        {{>topbar-hidden}}
        {{>new-org-invite-header}}
        {{>new-org-add-members}}

    </div>

</div>
